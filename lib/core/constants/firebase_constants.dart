class FirebaseConstants {
  FirebaseConstants._();

  // Your Firebase project uses a named Firestore database: "default".
  // The special SDK fallback "(default)" does not exist in this project.
  static const firestoreDatabaseId = 'default';
}
